# Paper errata — Oesterheld & Conitzer (2022), *Safe Pareto Improvements for Delegated Game Playing*

Defects in the committed source (`oesterheld-conitzer-2022-spi.txt`, the JAAMAS-equivalent
preprint) found while formalizing. Line numbers refer to the extraction. "Statement-level"
means the printed statement or definition is changed by the repair and the Lean carrier
discloses it; "proof-level" means only the printed argument is affected. Items were first
listed in `scoping.md` §5 and cross-checked by the codex review of 2026-09-04
(`codex-review-2026-09-04.md`); D6 and D9 were revised by that review.

| # | Where | Defect | Level | Repair in Lean |
|---|---|---|---|---|
| D1 | Definition 1, l. 349–352 | Strictness clause reads `uᵢ(Π(Γˢ)) > uᵢ(Π(Γˢ))`; the second occurrence must be `Π(Γ)`. | statement (typo) | `Play.IsStrictSPI` compares `Π(Γˢ)` with `Π(Γ)`. |
| D2 | (a) Definition 4, l. 763–766; (b) Lemma 4, l. 919–920 | Definition 4 writes `Γ ∼_Φ Γ'` for `Γ ∼_Φ Γˢ` and types `Φ` with an ordinary arrow `A → Aˢ` although `Φ` is multivalued. Lemma 4 applies "Pareto-improving" to isomorphisms between arbitrary `Γ, Γ'`, though Definition 4 defines it only when `Γ'` is a subset game of `Γ` (payoffs are compared under `Γ`'s `u`). | statement | `Play.ParetoImprovingCorrespondence` is typed on the two profile sets; `GameIso.ParetoImproving` compares under the source game's total payoff `u` (`dd:total-utility`), which is what Lemma 4 needs and is defined for any target. |
| D3 | Theorem 3 proof, l. 777–792 | Quantifies "for `i = 1, 2`" in an `n`-player theorem; the ⇒ direction opens with the SPI inequality reversed. | proof | — |
| D4 | (a) l. 732–734; (b) l. 1103–1106; (c) l. 1121–1126 and 1134 | Lemma 2.7's proof cites "reflexivity (Lemma 2.1)" for symmetry (2.2). Proposition 6's proof writes `Ψ(Φ(Γˢ))` for `Ψ(Φ(a₁, a₂))`. Proposition 7's proof (c) eliminates "Player 1's `R₁`" and "Player 2's `R`": the eliminated actions are player 1's `R` and then player 2's `F` (`T` dominates `R`; with `R` gone, `C` dominates `F`), and its final clause (l. 1134) excludes `(R, F)` from the empty-image set where it must exclude `(T, C)`, whose image the preceding clause declares nonempty. | proof | `Examples/Temptation.lean` eliminates `R` then `F`. |
| D5 | §2, l. 272–278 | Game isomorphism: `λ ∈ ℝⁿ₊` is ambiguous between `≥ 0` and `> 0`, and bijectivity of the `Φᵢ` is not stated. Both are needed: with `λᵢ = 0` allowed, isomorphism is not symmetric (a constant-payoff two-action game maps onto a game with payoffs `0, 1` by `u = 0·u' + 0`, with no positive-affine inverse); without bijectivity a one-action game "is isomorphic to" a two-action game with the same payoffs, and Appendix C invokes `Φ⁻¹` and "bijectivity of `Φ, Ψ`". | statement | `GameIso` has `scale_pos : 0 < λᵢ` and `bijOn : BijOn Φᵢ Aᵢ A'ᵢ` (`dd:iso`). |
| D6 | Lemma 13 proof, l. 1449–1451 | "WLOG assume that `Φ` simply maps `aᵢ ↦ âᵢ`": Assumption 2 supplies *some* isomorphism, and the hats are fresh tokens, so this is a relabeling of the tokens along the supplied isomorphism — legitimate, but the argument is omitted. (Revised from a claimed statement defect after the codex review.) | proof | Discharged: `TokenGame.reassign` defines `uᵉ` along the isomorphism Assumption 2 supplies (`Play.exists_tokenGame_ue_eq`, RULING 10), and Lemma 13 (`Representatives.exists_reassignment_condExp_eq`) is proved that way. |
| D7 | Lemma 13, l. 1443–1447; Corollary 14, l. 1480–1485 | Lemma 13's conditional expectations `E[· \| Π(Γ) = a]` are asserted for every `a ∈ A`, including outcomes of probability zero, where they are undefined. Corollary 14 calls an `n`-player set a "convex polygon". | statement | Rendered: Lemma 13 is stated on the support (`Representatives.support`, conditional expectation `Representatives.condExp` defined through `ProbabilityTheory.cond` on fibers of positive measure); Corollary 14 is carried as a convex polytope (`isPolytope_achievable`) with the weighted Minkowski-sum formula (`achievable_eq_improvementSum`). |
| D8 | Proposition 18 proof, l. 2069–2077 | "`exec(c₋ᵢ, c'ᵢ)` **is** the threat point of Player `i`": an arbitrary deviating program need not best-respond, so the deviator's payoff is *at most* `vᵢ`; and the bound requires the deviator's realized action to be independent of the punishers' randomization, which the paper's execution model does not specify. | statement (Prop 18) | `Prog.algorithm2_isProgramEquilibrium` via `ProgramGame.isProgramEquilibrium_of_algorithm2`: the deviator's payoff is bounded by `vᵢ` (`Game.expected_minimax_le_threatPoint`), and independence is the execution kernel's product structure (`dd:exec-kernel`). |
| D9 | Lemma 21, l. 2289–2312 | `Γ'ₘ = Γₘ` should read `Γ'ₘ = Γₖ` (the chain ends at `Γₖ`); the concise restatement says "`Γˢ'ʳᵉᵈ` is isomorphic to `Γˢ'ʳᵉᵈ`" for "… isomorphic to `Γʳᵉᵈ`". (Revised after the codex review: the page-break split originally listed here is not a defect.) | statement (typos) | Rendered by the derivation system: `Game.Deriv.exists_normalForm` reads the chain as ending at `Γₖ` and the isomorphism as between the two reductions. |
| D10 | Definition 7, l. 1356–1358 | Named "the **strict** full-coordination SPI decision problem" but its body asks only for a perfect-coordination SPI; Proposition 12, which it serves, is about strict SPIs. | statement | Ruled 2026-09-12 (RULING 7): the carrier reads "strict" into the body; Proposition 12 and Algorithm 1 concern strict SPIs. |
| D11 | Proposition 23, l. 2362–2377; Lemma 19 discussion, l. 2227–2229 | Proposition 23 sits in the omnilateral subsection D.2.1 and its algorithm builds an omnilateral SPI, but its statement says "unilateral". The discussion after Lemma 19 says the lemma does not prove "path dependence" where independence is meant. | statement (Prop 23) / typo | `Reduction.lean` carries Lemma 19 and proves path independence. |
| D12 | Theorem 15, l. 1497–1529 | `PF(C)` is the strong Pareto frontier and the theorem projects `x₁ᵐⁱⁿ, x₁ᵐᵃˣ, x₂ᵐⁱⁿ, x₂ᵐᵃˣ` onto `PF(C(Γ))` via `πᵢ(x, PF(C(Γ)))`, which exists only if `x` is a coordinate of a frontier point. Counterexample: a `2×2` game with payoffs `(0,0), (1,0), (0,1), (1,1)` has `C(Γ) = [0,1]²`, `PF = {(1,1)}`; if `Π(Γ)` surely plays the `(0,0)` outcome, Case A's premise holds but `π₁(0, PF)` does not exist. The paper's own existence remark ("such a `y'` exists iff `x` is `i`'s utility in some feasible payoff vector") is about `πᵢ(x, C(Γ))`. | statement | Ruled 2026-09-12: Theorem 15 **deferred** — no carrier until the projection reading is settled; the counterexample stands as recorded. |
| D13 | Definition 5, l. 1194–1213 (found in harness round 1: R1-F18/F22/F28, codex adjudication) | **The non-triviality clause is satisfied by every payoff shift or positive rescaling of a subset game**, so the (unilateral) SPI decision problem as printed is constant-true, contradicting the intent of Theorem 9: a subset game may change utilities (§2), item 1 only forbids *equality* of the reduced games (and explicitly allows isomorphic ones), and item 3 compares under the original `u` on both sides; so `Γˢ := (Aʳᵉᵈ, u + 1)` with the identity isomorphism (scale 1, shift −1) is a "non-trivial" SPI of every game. The strict variant is unaffected. Appendix D's hardness proof (l. 2710) treats the identity *action* map as the trivial case, which is the intended reading. | statement | Ruled 2026-09-12: the carrier `Game.SPIDecision` (and the unilateral variant) uses non-triviality = *the reduced action sets differ* (`dd:nontrivial`); the printed clause is carried alongside as `SPIDecisionPrinted` with the theorem that it is constant-true. The repaired predicates are two-sided: yes-instances `Examples.demandGame_spiDecision` / `demandGame_strictSPIDecision` / `complicatedTemptation_unilateralSPIDecision`, no-instance `not_spiDecision_of_card_le_one` (harness round 2). |
| D15 | Algorithm 2, l. 2052–2058 (found while formalizing Appendix A) | Line 3 says "Play `minimax(i, j)`", but by the paper's own definition (l. 1985–1993) `minimax(i, j) ∈ Δ(Aⱼ)` is *player `j`'s* strategy in the minimiser against `i`; the punisher `i` must play her own coordinate of the minimax profile against the deviator `j`, i.e. `minimax(j, i) ∈ Δ(Aᵢ)`. The proof of Proposition 18 ("`exec(c₋ᵢ, c'ᵢ)` is the threat point of Player `i`") uses the corrected reading. | statement (Algorithm 2) | `Prog.algorithm2` punishes a differing `j` with `Γ₀.minimax j i`. |
| D16 | Proposition 16 proof sketch, l. 1653–1660 | Writes `u(Π(Γˢ))` and `E[u(Π(Γˢ))]` where Definition 6 requires `uᵉ(Π(Aˢ, uˢ))`: `Γˢ` there is a perfect-coordination token game, on whose outcomes `u` is not defined. The printed argument also routes through "`(3,3)` is the unique sum-maximizer of `C(Γ)`, so `u(Π(Γˢ)) = (3,3)` with certainty". | proof (notation) | The carrier states the expectation with `uᵉ`; the proof uses two supporting half-planes of `C(Γ)` and linearity of expectation instead of the uniqueness step (both correct; the Lean route is shorter). |
| D17 | Appendix D.2.1, l. 2364–2375 (found while formalizing Proposition 23) | The omnilateral algorithm performs no non-triviality check: the identity injections `Φᵢ = id` are a legal guess, they are (weakly) Pareto-improving, and the game they build is `reduce Γ` itself — so the algorithm returns *True* on every game, although Definition 5 item 1 excludes exactly this candidate under either reading of non-triviality. The unilateral algorithm (D.2.2) has the same gap. | statement (Propositions 23 and 25) | The certificate check `Game.Certificate.Nontrivial` (in the repaired reading `dd:nontrivial`) is added to both algorithms; `Certificate.refl` with `refl_paretoImproving` and `not_refl_nontrivial` records the defect as a Lean fact, and `Examples.oneAction_not_certificate` exercises it. |
| D18 | Appendix D.3, Table 9 vs. the displayed formulas, l. 2503–2559 (found while formalizing Lemma 28) | The printed formula for `u₁` gives `ε` in the corner columns `2n+1, 2n+2` against every row `i ∈ [2n]`, and `u₂` gives `ε` in the corner rows against every column `j ∈ [2n]`; Table 9 prints `0` at all eight positions. The disagreement is material: under the formula, player 2 receives exactly `ε` against the rows `2n̂+1` and `2n̂+2` in *every* column of `Γ̂`, so no column of the unilateral candidate `Γˢ` is ever strictly dominated once those rows are present, and the proof's step "`(P, 2n̂+1)` and `(P, 2n̂+2)` strictly dominate `(P, i)`", which it justifies by "inspecting Table 9", fails — the unilateral half of Lemma 28's first claim is then false. Under the table every step of the printed proof checks. | statement (Table 9 / the reduction) | `Hardness.tableU₁` and `tableU₂` follow the **table** (`0`); RULING 15. The proof's item (d) also writes `uᶜ₂((T, i), (R, i))` and `Ψ₂(R, i)` for `(D, i)`, a notation slip. |
| D19 | Lemma 13's display, l. 1443–1447; Corollary 14, l. 1480–1485 (final audit, rediscovered independently by two channels) | Both write the base game's `u` on token outcomes — `E[u(Π(Γˢ)) \| Π(Γ) = a]` and `{E[u(Γ′)] \| Γ′ a perfect-coordination SPI}` — where `uᵉ` (resp. the token game's own assignment) is meant; `u` is not defined on fresh tokens (`Aˢᵢ ∩ Aᵢ = ∅`), and the proof itself writes `uᵉ(â) = E[u′(Π(Γ′)) \| …]`. The same slip as D16 for Proposition 16. Line 1458 also asserts `u(a) ≤ …` for every fixed `a` unconditionally, where the SPI supplies it only on the fiber `Π(Γ) = a`. | notation | The carriers use `T.ue` and `tokenValue = ∫ uᵉ(Π(T.game))`; Lemma 13's conditional comparison is stated on the support with the conditional-measure order. |
| D20 | Lemma 20, l. 2261–2282 | The cancellation alternative reads "either `Γ = Γ̂`"; it must be `Γ = Γ̃` (the two one-step reducts coincide), as the proof's own first sentence ("if `ã = a` then `Γ = Γ̃`") says. The second proof bullet says "`Γ̃` is obtained from `Γ̂` by removing `a`" for "removing `ã`". | statement (typo) | Carried with the correction: `Game.elim_diamond` states the first alternative as `Γ₁ = Γ₂`, the two reducts coinciding. |
| D21 | Appendix D.3, l. 2508 (final audit) | The hardness construction fixes only `ε < 1/2n`, allowing `ε ≤ 0`; with `ε = 0` even Table 9's reading fails, since the corner rows then pay player 2 `0` everywhere and `(P, 2n̂+1)` strictly dominates nothing against them, so the unilateral half of Lemma 28's first claim needs `0 < ε`. | statement (missing hypothesis) | `Hardness.subgraphIsoProblem_iff_*` assume `0 < ε` explicitly and say so. |
| D22 | §4.4.2 prose, l. 1028–1031 (final audit) | "in the 2-player case [iterated removal of strictly dominated strategies] always eliminates exactly the non-rationalizable strategies": true for domination by *mixed* strategies (Pearce), false for the paper's pure-strategy dominance — a row paying `(3, 0)`, `(0, 3)`, `(1, 1)` against two columns is never a best response but is pure-dominated by nothing. | prose | No node depends on it; `Game.StrictlyDominates` is pure dominance, as the paper's Assumption 1 requires. |
| D23 | §2, l. 230–233 (final audit) | Games are defined with an arbitrary set `A` of profiles; finiteness is assumed silently everywhere (payoff matrices, the attained extrema in Appendix C's proof of Lemma 4 — over `A = ℤ` with `u(a) = a` both `a ↦ a ± 1` are positive-affine automorphisms and only one is Pareto-improving). | statement (missing hypothesis) | `Game.S i : Finset (𝒜 i)`, nonempty, as the §2 standing assumptions say. |
| D24 | Lemma 28, second claim, l. 2705–2769 (final audit) | Item (a) says "outside `AΓ̂` there is no outcome with utility at least 6 for Player 1" — false, `T × P` pays player 1 `10`; the argument must also use player 2's `−10` there. Item (c) reverses an inequality against `−10` (l. 2731) and needs `n ≥ 2`; items (c)–(d) write `n + i` for `n̂ + i` (l. 2688), the source `n` for the target `n̂` (l. 2736, 2748), `(R, i)` for `(D, i)` (l. 2747–2750), and `ϕ(a)` for `ϕ(i)` (l. 2761). | proof | The Lean route (`Hardness.subgraphIsoProblem_of_spiDecision`) is organised around the product structure of the certificate and uses none of these steps. |
| D14 | Lemma 21, l. 2289–2300 (codex adjudication collateral, R1-F39) | The length bound `m ≤ k` on the reorganized chain is false: for one player with actions `{0, 1, 2}` and `u(a) = a`, eliminating action `0` is a chain of two games, but a normal-form chain must reach the full reduction `{2}` from both ends, i.e. five games. | statement (Lemma 21's bound) | The bound is not rendered; the wrapper theorems carry the qualitative shape only and say so. |

Not defects, but readings the formalization had to fix and discloses (see the `dd:` glossary
in `SafeParetoImprovements.lean`): the quantification domain of Assumptions 1–2
(footnote 6, `dd:universe`); the joint sample space behind "with certainty"
(`dd:representatives`); the informal consistency argument of §4.4.3 (`dd:book`, now a
theorem).
