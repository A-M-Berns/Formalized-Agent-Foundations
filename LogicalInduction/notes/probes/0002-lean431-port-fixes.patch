--- /private/tmp/claude-501/-Users-anson/19165699-21a2-4e79-b4fd-83a1d4cd6f10/scratchpad/complexitylib/Complexitylib/Asymptotics.lean	2026-08-24 11:56:18
+++ /private/tmp/claude-501/-Users-anson/19165699-21a2-4e79-b4fd-83a1d4cd6f10/scratchpad/port/Complexitylib/Asymptotics.lean	2026-08-24 12:03:53
@@ -101,18 +101,13 @@
 theorem BigO.add {f₁ f₂ g : ℕ → ℕ} (h₁ : f₁ =O g) (h₂ : f₂ =O g) :
     (fun n => f₁ n + f₂ n) =O g := by
   show (fun n => ((f₁ n + f₂ n : ℕ) : ℝ)) =O[atTop] _
-  have key := IsBigO.add h₁ h₂
-  convert key using 1
-  ext n; push_cast; ring
+  simpa only [Nat.cast_add] using IsBigO.add h₁ h₂
 
 /-- Product of two big-O bounds: `f₁ = O(g₁) → f₂ = O(g₂) → (f₁·f₂) = O(g₁·g₂)`. -/
 theorem BigO.mul {f₁ f₂ g₁ g₂ : ℕ → ℕ} (h₁ : f₁ =O g₁) (h₂ : f₂ =O g₂) :
     (fun n => f₁ n * f₂ n) =O (fun n => g₁ n * g₂ n) := by
   show (fun n => ((f₁ n * f₂ n : ℕ) : ℝ)) =O[atTop] (fun n => ((g₁ n * g₂ n : ℕ) : ℝ))
-  have key := IsBigO.mul h₁ h₂
-  convert key using 1
-  · ext n; push_cast; ring
-  · ext n; push_cast; ring
+  simpa only [Nat.cast_mul] using IsBigO.mul h₁ h₂
 
 /-- Raising both sides of a big-O bound to a fixed natural power preserves
 big-O. -/
@@ -157,9 +152,7 @@
 theorem LittleO.add {f₁ f₂ g : ℕ → ℕ} (h₁ : f₁ =o g) (h₂ : f₂ =o g) :
     (fun n => f₁ n + f₂ n) =o g := by
   show (fun n => ((f₁ n + f₂ n : ℕ) : ℝ)) =o[atTop] _
-  have key := IsLittleO.add h₁ h₂
-  convert key using 1
-  ext n; push_cast; ring
+  simpa only [Nat.cast_add] using IsLittleO.add h₁ h₂
 
 /-- Constant multiple preserves little-o. -/
 theorem LittleO.const_mul_left (c : ℕ) {f g : ℕ → ℕ} (h : f =o g) :
@@ -212,9 +205,7 @@
     ext n; push_cast; ring
   have hf₂ : (fun n => ((f₂ n : ℕ) : ℝ)) =O[atTop]
       (fun n => ((T₁ n + T₂ n : ℕ) : ℝ)) := IsBigO.trans ho₂ (le_add_right T₁ T₂)
-  have := IsBigO.add hcf₁ hf₂
-  convert this using 1
-  ext n; push_cast; ring
+  simpa only [Nat.cast_add] using IsBigO.add hcf₁ hf₂
 
 -- ════════════════════════════════════════════════════════════════════════
 -- BigO max and power bounds
--- /private/tmp/claude-501/-Users-anson/19165699-21a2-4e79-b4fd-83a1d4cd6f10/scratchpad/complexitylib/Complexitylib/Mathlib/NatBits.lean	2026-08-24 11:56:18
+++ /private/tmp/claude-501/-Users-anson/19165699-21a2-4e79-b4fd-83a1d4cd6f10/scratchpad/port/Complexitylib/Mathlib/NatBits.lean	2026-08-24 12:06:12
@@ -95,7 +95,7 @@
         Nat.fromBits rest + (if bit then 1 else 0) * 2 ^ rest.length := by
       simp only [Nat.fromBits]
       exact Nat.add_comm _ _
-    simp only [Nat.toBits, List.cons.injEq]
+    simp only [List.length_cons, Nat.toBits, List.cons.injEq]
     constructor
     · rw [hval, Nat.add_mul_div_right _ _ (Nat.two_pow_pos _), Nat.div_eq_of_lt hlt]
       cases bit <;> simp
--- /private/tmp/claude-501/-Users-anson/19165699-21a2-4e79-b4fd-83a1d4cd6f10/scratchpad/complexitylib/Complexitylib/Models/TuringMachine/Hoare.lean	2026-08-24 11:56:18
+++ /private/tmp/claude-501/-Users-anson/19165699-21a2-4e79-b4fd-83a1d4cd6f10/scratchpad/port/Complexitylib/Models/TuringMachine/Hoare.lean	2026-08-24 12:06:12
@@ -57,7 +57,7 @@
   obtain ⟨c₂, t₂, ht₂, hreach₂, hhalt₂, hpost⟩ := h₂ _ _ _ hmid'
   refine ⟨phase2Wrap tm₁ tm₂ c₂, t₁ + 1 + t₂, ?_, ?_, ?_, ?_⟩
   · omega
-  · convert seqTM_reachesIn_of_reachesIn tm₁ tm₂ hreach₁ hhalt₁ hreach₂ using 1
+  · exact seqTM_reachesIn_of_reachesIn tm₁ tm₂ hreach₁ hhalt₁ hreach₂
   · rw [phase2Wrap_halted_iff]; exact hhalt₂
   · exact hpost
 
--- /private/tmp/claude-501/-Users-anson/19165699-21a2-4e79-b4fd-83a1d4cd6f10/scratchpad/complexitylib/Complexitylib/Models/TuringMachine/UTM/Internal/HaltTest.lean	2026-08-24 11:56:18
+++ /private/tmp/claude-501/-Users-anson/19165699-21a2-4e79-b4fd-83a1d4cd6f10/scratchpad/port/Complexitylib/Models/TuringMachine/UTM/Internal/HaltTest.lean	2026-08-24 14:34:40
@@ -598,7 +598,7 @@
           rw [hw₁] <;> omega
     | cons b B' =>
       have hr4 : (c.work 4).read = (b).toΓ := by
-        rw [Tape.read, hh4]; simpa using hc4 0 (by simp)
+        rw [Tape.read, hh4]; exact hc4 0 (by simp)
       have hneq : (c.work 3).read ≠ (c.work 4).read := by
         rw [hr3, hr4]
         exact fun h => toΓ_ne_blank (hB b (by simp)) h.symm
@@ -612,7 +612,7 @@
   | cons a A' ihA =>
     intro hA B hB c p q hstate hh3 hh4 hc3 hb3 hc4 hb4 hin hout hoth
     have hr3 : (c.work 3).read = a.toΓ := by
-      rw [Tape.read, hh3]; simpa using hc3 0 (by simp)
+      rw [Tape.read, hh3]; exact hc3 0 (by simp)
     have hr3nb : (c.work 3).read ≠ Γ.blank := by
       rw [hr3]; exact toΓ_ne_blank (hA a (by simp))
     have hr3ns : (c.work 3).read ≠ Γ.start := by
@@ -633,7 +633,7 @@
           rw [hw₁] <;> omega
     | cons b B' =>
       have hr4 : (c.work 4).read = b.toΓ := by
-        rw [Tape.read, hh4]; simpa using hc4 0 (by simp)
+        rw [Tape.read, hh4]; exact hc4 0 (by simp)
       by_cases hab : a = b
       · subst hab
         have heq : (c.work 3).read = (c.work 4).read := by rw [hr3, hr4]
@@ -649,7 +649,7 @@
             (by
               intro j hj
               rw [hcl3, show p + 1 + j = p + (j + 1) by omega]
-              simpa using hc3 (j + 1) (by simpa using Nat.succ_lt_succ hj))
+              exact hc3 (j + 1) (by simpa using Nat.succ_lt_succ hj))
             (by
               rw [hcl3, show p + 1 + A'.length = p + (a :: A').length by
                 simp only [List.length_cons]; omega]
@@ -657,7 +657,7 @@
             (by
               intro j hj
               rw [hcl4, show q + 1 + j = q + (j + 1) by omega]
-              simpa using hc4 (j + 1) (by simpa using Nat.succ_lt_succ hj))
+              exact hc4 (j + 1) (by simpa using Nat.succ_lt_succ hj))
             (by
               rw [hcl4, show q + 1 + B'.length = q + (a :: B').length by
                 simp only [List.length_cons]; omega]
--- /private/tmp/claude-501/-Users-anson/19165699-21a2-4e79-b4fd-83a1d4cd6f10/scratchpad/complexitylib/Complexitylib/Models/TuringMachine/UTM/Internal/BodyLoop.lean	2026-08-24 11:56:18
+++ /private/tmp/claude-501/-Users-anson/19165699-21a2-4e79-b4fd-83a1d4cd6f10/scratchpad/port/Complexitylib/Models/TuringMachine/UTM/Internal/BodyLoop.lean	2026-08-24 14:35:01
@@ -304,7 +304,6 @@
         else (c.work scT).cells j)
       (by
         intro i hi
-        dsimp only
         by_cases hcase : 1 ≤ i ∧ i < 1 + stSyms.length
         · rw [if_pos hcase]
           exact hWns _ (by omega)
@@ -909,7 +908,6 @@
               else (c.work scT).cells j)
             (by
               intro i hi
-              dsimp only
               by_cases hcase : 1 ≤ i ∧ i < 1 + stSyms.length
               · rw [if_pos hcase]
                 exact hWns _ (by omega)
--- /private/tmp/claude-501/-Users-anson/19165699-21a2-4e79-b4fd-83a1d4cd6f10/scratchpad/complexitylib/Complexitylib/Models/TuringMachine/SingleTape/Internal/Correctness.lean	2026-08-24 11:56:18
+++ /private/tmp/claude-501/-Users-anson/19165699-21a2-4e79-b4fd-83a1d4cd6f10/scratchpad/port/Complexitylib/Models/TuringMachine/SingleTape/Internal/Correctness.lean	2026-08-24 14:36:59
@@ -1778,7 +1778,8 @@
       (fun j => if (j : ℕ) < m then false else rc j) ilm (if m = 0 then false else true)
       ((singleTapeSim N).trace (3 * m) (fun _ => bb) c1) hcs (by rw [hcw]; exact hh)
       (by rw [hcw]; exact hb0) (by rw [hcw]; exact hb1) (by rw [hcw]; exact hb2) hnt hcis hcos
-    rw [hcw, hrcm] at htr
+    rw [hcw] at htr
+    simp only [hrcm] at htr
     refine ⟨⟨wtm.head + 3, Function.update (Function.update (Function.update wtm.cells wtm.head
         (if rc ⟨m, hmk⟩ then Γw.one else Γw.zero).toΓ) (wtm.head + 1) Γw.zero.toΓ)
         (wtm.head + 2) Γw.zero.toΓ⟩, ?_, ?_, ?_, ?_, ?_⟩
