/-!
# Palomar challenge — Robust Cooperation in the Prisoner's Dilemma

**Not yet written.**  This is a scaffolding stub: it carries no statement, and the
`comparator.json` beside it names a placeholder in `theorem_names`.

When this entry is written, this module must state the compared results and nothing
else.  Its transitive import closure may contain **only** Lean core, Mathlib, Tau Ceti
and CSLib — never a `ModalAgents` module, never `Foundation`, never `complexitylib`.
`scripts/check_palomar_wiring.py` enforces that.  Statements are `sorry`-ed here; the
proofs live in `Palomar.ModalAgents.Solution`.

See `Palomar/README.md` for the layout and the module-name scheme.
-/
