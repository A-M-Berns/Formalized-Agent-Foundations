# Round 11 cross-family sweeps — re-run of 2026-08-30

**What this is.** Round 11 (2026-08-17) ran single-family: the two codex sweeps in
`round-11-adjudication-prompts.md` failed at invocation on plan quota. This file records
their re-run on 2026-08-30, by GPT-5.6 Sol (`codex exec --sandbox read-only`, high
reasoning effort), against the worktree head carrying the 2026-08-30 fixes (Example 4
brought into scope, FSM Theorem 6.2 binder minimized — the latter outside this library).
Both prompts were re-issued with the scope ruling updated (97 of 98 nodes, Example 3 out)
and the statement extracts regenerated by `extract_statements.py` from the same head.
Both logs show genuine file-read activity and end in a JSON findings array (the validity
test of the harness's codex-log discipline); transcripts were reviewed by the
orchestrator and the arrays are summarized below.

**Why sweeps, not per-finding adjudication.** The 38 round-11 findings were fixed in
`4cbb1a7`; adjudicating them per-finding in 2026-08-30 would adjudicate superseded text.
The re-run instead applied the same two lenses to the whole current surface, which is the
coverage the parked prompts were written to provide. The external GPT-5.6 Sol review of
2026-08-17 already provided whole-artifact cross-family coverage of `4cbb1a7`; this
re-run extends that to the amended scope and the regenerated statement surface.

## Statement sweep (Lens A mirror): 10 findings

- 3 findings flagged prose written earlier the same day (scope-amendment text not yet
  propagated to `InfiniteExamples.lean`'s module doc, one README passage, and the
  FFS-INVENTORY preamble). All fixed in this change; the sweep read the tree mid-edit.
- 1 MAJOR flagged the module doc's claim that "every §3–§4 statement carries
  `[Finite F.B]`" — false (the definitions and e.g. Propositions 10, 11, 24 carry none).
  Fixed.
- 5 paper errata, recorded as E15–E18 in `paper-errata.md` (Proposition 3's proof
  assumes finiteness its statement does not; Proposition 21's proof invokes a false
  characterization of subpartition generation, sharpening E9; Theorem 2's weak-union
  proof applies Lemma 1 to blocks disjoint from `z`; Proposition 30's proof omits the
  `Irr^F(E) = ∅` case) plus an independent reconfirmation of E7 (Proposition 34's ∩ for
  ∪) and a MINOR batch of referential typos subsumed by existing rows (E4, E10, E14).
- **No finding against any Lean statement.**

## Integrity sweep (Lens B mirror): 18 findings

- 4 were the same mid-edit stale-scope prose as above; fixed.
- 3 witness-register corrections, all accepted and fixed: the README's Theorem 2
  paragraph misstated the non-degenerate witness's parameters and denied the contraction
  and composition instances that `Examples.lean` carries; the FFS-INVENTORY comment
  contradicted itself the same way; the README's Definition 47 row overstated the
  non-constant `ObservesPartition` witness (its clause-2 obligations discharge for
  structural reasons; clause 2 is metered at Definition 46).
- 1 accepted documentation fix: `API.lean` identified Mathlib `Irreducible` with the
  paper's support-based notion; the glossary's own "strictly stronger, agree on nonzero
  non-units" wording now appears there too.
- 1 accepted surface-hygiene fix: `finSupp_ne_top` and `orthogonal_finSupp_self` were
  `lemma`s carrying `Paper node:` lines; both are now `theorem`s and the label linter
  accepts `Example n` for this library.
- 2 wontfix, dispositions recorded here: Proposition 29's unused `E.Nonempty` is kept
  deliberately for paper fidelity (its docstring says so, and
  `Examples.irr_partition_holds_at_empty` documents the `E = ∅` case);
  `Examples.commonRefinement_singleton` is a one-line named reading of `sInf_singleton`
  kept because the witness file states facts in the paper's `⋁_S` vocabulary.
- 7 paper errata, duplicating the statement sweep's (recorded once, above).
- **No finding against any Lean statement or proof.**

**Verdict carried forward.** Cross-family coverage of the current surface: PASS at the
statement and integrity level; every accepted finding was documentation or register
accuracy and is fixed in this change. The one remaining round-11 obligation is Anson's
deferred human read-through (`statement-readthrough-checklist.md`).
